<p id="notice"><%= notice %></p>
<p>
  <strong>Profile Image:</strong><br />
  <% if @user.avatar? %>
    <%= image_tag @user.avatar.url, :size => "200x200", :crop => :fill %>
    <% else %>
    <img src="/user_missing.png" width="200" height="200" alt="profile_image">
<% end %>
</p>
<p>
  <strong>User name:</strong>
  <%= @user.user_name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>
<p>
  

</p>


<% if admin_logged_in? && !@user.isAdmin%>
  <h1><%= link_to 'Delete Profile', @user, method: :delete, data: { confirm: 'Are you sure?' } %></h1>
<% end %>


</div>
